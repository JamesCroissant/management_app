<p id="notice"><%= notice %></p>

<p>
  <strong>車名 : </strong>
  <%= @car.car_name %>
</p>


<p>
  <strong>持ち主 : </strong>
  <%= @car.user.nickname %>
</p>

<p>
  <strong>車のナンバー : </strong>
  <%= @car.car_number %>
</p>

<p>
  <strong>車の種類 : </strong>
  <%= @car.car_model %>
</p>

<% if user_signed_in? && current_user.id == @car.user_id %>
  <%= link_to '編集', edit_car_path(@car.id) ,method: :get %> |
  <%= link_to '削除', car_path(@car.id), method: :delete, data: { confirm: 'Are you Sure?' } %> |
<% end %>
<%= link_to '戻る', root_path %>